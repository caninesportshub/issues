policy:
  section:
    id: platform
    label:
      - name: web
        keys:
          - Web (browser-based)     # Exact match – copy-paste the full option text from your form
      - name: mobile
        keys:
          - Phone/Tablet (mobile app)   # Exact match for the other option
